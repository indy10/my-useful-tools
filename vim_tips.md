# VIM Tip 모음

> 출처: [VIM tip 모음](https://sonumb.tistory.com/138) (2022.01.05)

---

## 1. 짧게 매칭되는 패턴 찾기

`.*` 는 최대 매칭(greedy)이라 원치 않는 범위까지 잡힐 수 있다.

```vim
" 예: malloc( sizeof( blahblab ) * 3 ) -> malloc( sizeof(blahblab) * 3 ) 로 바꾸고 싶을 때
:%s/sizeof( \(.*\) )/sizeof(\1)/g
" 위 명령은 greedy 매칭 때문에 실제로는 malloc( sizeof(blahblab ) * 3) 처럼 잘못 치환될 수 있음

" 가장 짧게 매칭(non-greedy)되도록 하려면 \{-} 사용
:%s/sizeof( \(.\{-}\) )/sizeof(\1)/g
```

---

## 2. 검색 시 대소문자 무시

### 2.1. 커맨드 모드에서 설정

```vim
:set ignorecase   " 또는 :set ic   → 대소문자 무시
:set noignorecase " 또는 :set noic → 대소문자 구분

:set smartcase    " 또는 :set scs
" 검색 문자열이 모두 소문자면 대소문자 구분 안 함
" 검색 문자열에 대문자가 하나라도 있으면 대소문자 구분함
" 단, smartcase가 동작하려면 ignorecase가 먼저 켜져 있어야 함
```

### 2.2. 검색 모드에서 즉시 적용

검색 명령 입력 중 `\c`를 붙이면 그 검색에 한해 대소문자 무시.

```vim
/\cmemory
```
→ `Memory`, `memory`, `MEMORY` 모두 검색됨.

---

## 3. 단축키 모음

### 3.1. 전역

| 명령 | 설명 |
|---|---|
| `:help keyword` | keyword에 대한 도움말 열기 |
| `:saveas file` | file로 저장하기 |
| `:close` | 현재 영역 닫기 |
| `K` | 커서 위치한 단어의 맨페이지 열기 |

### 3.2. 커서 이동

| 명령 | 설명 |
|---|---|
| `h` / `j` / `k` / `l` | 왼쪽 / 아래 / 위 / 오른쪽 이동 |
| `H` / `M` / `L` | 화면 상단 / 중간 / 하단으로 이동 |
| `w` / `W` | 다음 단어 시작으로 점프 (W는 문장부호 포함 한 단어로 취급) |
| `e` / `E` | 다음 단어 끝으로 점프 (E는 문장부호 포함) |
| `b` / `B` | 이전 단어 시작으로 점프 (B는 문장부호 포함) |
| `%` | 짝이 맞는 괄호로 이동 (`:h matchpairs` 참고) |
| `0` / `^` / `$` | 행의 처음 / 시작 문자 / 끝으로 점프 |
| `g_` | 행의 끝 문자로 점프 |
| `gg` / `G` | 문서 첫 행 / 마지막 행으로 점프 |
| `5G` 또는 `:5` | 5번째 행으로 점프 |
| `fx` / `Fx` | 오른쪽 / 왼쪽에서 가장 가까운 문자 x로 이동 |
| `tx` / `Tx` | 오른쪽 / 왼쪽 문자 x 바로 앞까지 이동 |
| `;` / `,` | 최근 f, t, F, T 명령 반복 / 반대 방향 반복 |
| `}` / `{` | 다음 / 이전 단락(함수·블록)으로 이동 |
| `zz` | 커서 행을 화면 중앙으로 |
| `Ctrl+e` / `Ctrl+y` | 커서 고정한 채 화면 한 줄 아래 / 위로 스크롤 |
| `Ctrl+b` / `Ctrl+f` | 한 화면 위 / 아래로 |
| `Ctrl+d` / `Ctrl+u` | 반 화면 아래 / 위로 |

> 팁: 이동 명령 앞에 숫자를 붙이면 그만큼 반복 (예: `4j` → 4행 아래로 이동)

### 3.3. 삽입 모드 - 텍스트 삽입/추가

| 명령 | 설명 |
|---|---|
| `i` / `I` | 커서 앞 / 행 시작에 삽입 |
| `a` / `A` | 커서 뒤 / 행 끝에 추가 |
| `o` / `O` | 현재 행 아래 / 위에 새 행 추가 |
| `ea` | 단어 끝에 추가 |
| `<Esc>` | 삽입 모드 종료 |

### 3.4. 편집

| 명령 | 설명 |
|---|---|
| `r` | 한 글자 바꾸기 |
| `J` / `gJ` | 다음 행과 연결 / 공백 없이 연결 |
| `gwip` | 단락 리플로우 |
| `cc` / `C` / `c$` | 행 전체 / 행 끝까지 새로 쓰기 |
| `ciw` / `cw` | 단어 전체 / 끝까지 새로 쓰기 |
| `s` / `S` | 한 글자 삭제 후 삽입 / 행 삭제 후 입력(`cc`와 동일) |
| `xp` | 두 문자 위치 바꾸기 |
| `u` / `Ctrl+r` | 실행 취소 / 다시 실행 |
| `.` | 마지막 명령 반복 |

### 3.5. 텍스트 선택 (비주얼 모드)

| 명령 | 설명 |
|---|---|
| `v` | 문자 단위 선택 모드 시작 |
| `V` | 행 단위 선택 모드 시작 |
| `Ctrl+v` | 블록 선택 모드 시작 |
| `o` / `O` | 선택 영역 반대쪽 끝 / 블록 반대쪽 모서리로 이동 |
| `aw` | 단어 선택 |
| `ab` / `aB` | `()` / `{}` 블록 선택 |
| `ib` / `iB` | `()` / `{}` 블록 내부만 선택 |
| `<Esc>` | 선택 모드 종료 |

### 3.6. 선택 모드 명령

| 명령 | 설명 |
|---|---|
| `>` / `<` | 텍스트 오른쪽 / 왼쪽으로 이동(들여쓰기) |
| `y` / `d` | 선택 텍스트 복사 / 삭제 |
| `~` | 대소문자 반전 |

### 3.7. 레지스터

| 명령 | 설명 |
|---|---|
| `:reg` | 레지스터 내용 표시 |
| `"xy` | 레지스터 x로 복사 |
| `"xp` | 레지스터 x 내용 붙여넣기 |

> 팁: 레지스터는 `.viminfo`에 저장되어 다음 vim 실행 시 복원됨. 0번 레지스터에는 항상 가장 최근 복사 내용이 들어있음.

### 3.8. 표시 (마크)

| 명령 | 설명 |
|---|---|
| `` :marks `` | 표시 목록 확인 |
| `ma` | 현재 위치를 마크 a로 설정 |
| `` `a `` | 마크 a 위치로 점프 |
| `` y`a `` | 마크 a 위치까지 복사 |

> `a` 자리에는 키보드상의 임의의 단일 문자를 쓸 수 있음.

### 3.9. 매크로

| 명령 | 설명 |
|---|---|
| `qa` | 매크로 a 기록 시작 |
| `q` | 매크로 기록 중지 |
| `@a` | 매크로 a 실행 |
| `@@` | 마지막 실행 매크로 재실행 |

### 3.10. 잘라내기와 붙여넣기

| 명령 | 설명 |
|---|---|
| `yy` / `2yy` | 행 복사 / 2줄 복사 |
| `yw` | 다음 단어 시작까지 복사 |
| `y$` | 행 끝까지 복사 |
| `p` / `P` | 커서 뒤 / 앞에 붙여넣기 |
| `dd` / `2dd` | 행 잘라내기 / 2줄 잘라내기 |
| `dw` | 다음 단어 시작까지 잘라내기 |
| `D` / `d$` | 행 끝까지 잘라내기 |
| `x` | 한 글자 잘라내기 |

### 3.11. 나가기

| 명령 | 설명 |
|---|---|
| `:w` | 저장(나가지 않음) |
| `:w !sudo tee %` | sudo 권한으로 저장 |
| `:wq` / `:x` / `ZZ` | 저장 후 나가기 |
| `:q` | 나가기 (변경사항 있으면 실패) |
| `:q!` / `ZQ` | 변경사항 버리고 나가기 |
| `:wqa` | 열린 모든 탭 저장 후 나가기 |

### 3.12. 찾기와 바꾸기

| 명령 | 설명 |
|---|---|
| `/pattern` / `?pattern` | 순방향 / 역방향 검색 |
| `\vpattern` | '매직' 모드: 영숫자 외 문자를 정규식 심볼로 해석 |
| `n` / `N` | 검색 반복 / 반대 방향 반복 |
| `:%s/old/new/g` | 전체 치환 |
| `:%s/old/new/gc` | 확인하며 치환 |
| `:noh` / `:nohlsearch` | 검색 강조 해제 |

### 3.13. 여러 파일에서 찾기

```vim
:vimgrep /pattern/ {file}
" 예: :vimgrep /foo/ **/*
```

| 명령 | 설명 |
|---|---|
| `:cn` / `:cp` | 다음 / 이전 일치 항목으로 이동 |
| `:copen` | 일치 목록을 새 창으로 열기 |

### 3.14. 여러 파일 작업

vim 실행 시 여러 파일명과 함께 `-o`(수평 분할) 또는 `-O`(수직 분할) 옵션을 쓰면 동시에 창으로 열 수 있음.

| 명령 | 설명 |
|---|---|
| `:e file` | 새 버퍼에서 파일 편집 |
| `:bnext` / `:bn` | 다음 버퍼 |
| `:bprev` / `:bp` | 이전 버퍼 |
| `:bd` | 버퍼 삭제(파일 닫기) |
| `:ls` | 열린 버퍼 목록 |
| `:sp file` | 상하 분할로 파일 열기 |
| `:vsp file` | 좌우 분할로 파일 열기 |
| `Ctrl+w s` / `Ctrl+w v` | 상하 / 좌우 창 분할 |
| `Ctrl+w w` / `Ctrl+w q` | 창 전환 / 창 닫기 |
| `Ctrl+w h` / `Ctrl+w l` | 왼쪽 / 오른쪽 창으로 이동 |
| `Ctrl+w j` / `Ctrl+w k` | 아래 / 위 창으로 이동 |

### 3.15. 탭

vim 실행 시 파일명들과 함께 `-p` 옵션을 쓰면 탭으로 열 수 있음.

| 명령 | 설명 |
|---|---|
| `:tabnew [file]` | 새 탭에서 파일 열기 |
| `Ctrl+w T` | 현재 분할 창을 새 탭으로 이동 |
| `gt` / `:tabnext` | 다음 탭 |
| `gT` / `:tabprev` | 이전 탭 |
| `#gt` | #번 탭으로 이동 |
| `:tabmove #` | 현재 탭을 #번째로 이동 (0부터 시작) |
| `:tabclose` | 현재 탭 닫기 |
| `:tabonly` | 현재 탭 외 모두 닫기 |
| `:tabdo command` | 모든 탭에 command 실행 (예: `:tabdo q`) |

---

## 요약

- **패턴 매칭**: `\{-}`로 non-greedy 매칭 가능
- **대소문자 무시**: `:set ignorecase`/`smartcase` (전역) 또는 `\c` (검색 시 즉시)
- **단축키 15개 카테고리**: 전역, 커서 이동, 삽입, 편집, 비주얼 선택, 선택 모드, 레지스터, 마크, 매크로, 잘라내기/붙여넣기, 나가기, 찾기/바꾸기, 다중 파일 검색, 다중 파일 작업, 탭
